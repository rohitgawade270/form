import { createTheme } from "@mui/material";


const theme = createTheme({
    typography: {
      allVariants: {
       fontWeight:400,
       fontSize:'12px'
      },
    },
  });

  export default theme;
  
  